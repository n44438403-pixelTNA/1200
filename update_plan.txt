1. Fix the RevisionHub `onNavigateContent` missing `targetSubject` issue in `components/StudentDashboard.tsx`
2. Wrap App initialization logic in `App.tsx` or similar file in try/catch or ensure crash resilience to "keep user data safe". Wait, let's check `App.tsx` for crash safety.
